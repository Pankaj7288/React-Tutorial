import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

const render = document.getElementById("root");
const element = (
  <>
    <App />
  </>
);

ReactDOM.render(element, render);
